# hun_proposal Quickstart Guide

hun_proposal은 나라장터의 실공고 선별부터 분석, 제안서(HTML/PPTX) 초안 생성까지의 전 과정을 자동화 및 보조하는 제안 자동화 종단 시스템입니다.

## 1. 시스템 개요
본 시스템은 다음과 같은 3계층 구조로 설계되어 있습니다:
- **app/**: 제안서 렌더링 엔진 및 SlideModel 바인딩 코어 (특정 도메인 노하우 배제)
- **packs/**: 디자인 및 템플릿 팩 — **기본은 `core`(중립 코어·무채)**. 옛 하우스 팩(house_a·house_b)은 W31에서 `<개발 원본 전용 경로>`로 이전됨(참조 금지, 레거시 재현 시만 되돌림)
- **projects/**: 실제 제안 프로젝트 데이터

전체 파이프라인 흐름:
RFP (공고문) → 공고 선별 → 첨부 분석 (stage 2/3) → SlideModel 바인딩 (stage 6/7/8) → 제안서 렌더링 → HTML / PPTX 산출

---

## 2. 실공고 선별 대시보드 실행
로컬 대시보드 서버를 실행하여 수집된 공고를 한눈에 모니터링하고 제안 여부를 결정할 수 있습니다. (무의존 Python 표준 라이브러리 서버)

```powershell
python dashboard/server.py
```
* 기본 포트 `8754`로 실행됩니다. 브라우저에서 `http://127.0.0.1:8754`로 접속하세요.
* 다른 포트를 사용하려면 `--port <포트번호>` 인자를 지정할 수 있습니다.

---

## 3. 대시보드 및 제안 프로세스 사용 흐름

### Step 1: 공고 검색 및 라이브 수집
* 대시보드 상단 검색창에 키워드를 입력하면 나라장터에서 실시간으로 라이브 수집(네트워크 검색)을 수행합니다.
* **AND/OR 불리언 검색**을 지원합니다:
  - `AI 플랫폼` 또는 `AI AND 플랫폼`: 두 키워드를 모두 포함 (AND)
  - `빅데이터 OR 클라우드`: 둘 중 하나를 포함 (OR)
  - `AI 플랫폼 OR 빅데이터 구축`: `(AI AND 플랫폼) OR (빅데이터 AND 구축)`
  - `&` 및 `|` 기호도 사용 가능합니다.

### Step 2: Go / Hold / Skip 선별
* 수집된 공고 리스트를 확인하고 각 공고에 대해 **Go / Hold / Skip** 여부를 토글합니다.
* 필요 시 메모를 입력하면 변경 즉시 `dashboard/feedback.json`에 반영됩니다.
* ⚠️ **2026-07-21부터 Go/Hold/Skip은 실행 트리거가 아니라 메모 기능입니다**(모니터링 요원의 검토 흔적). 실제 착수는 Step 4를 보세요 — 공고 고유번호를 복사해 채팅에 붙여넣는 것이 시작 통로입니다.

### Step 3: 공고 및 RFP 분석 (stage 2/3)
1. 각 공고의 **📋 분석** 버튼을 클릭하여 모달창을 엽니다.
2. **stage 2 (정규화)**: 서버가 공고 첨부파일(HWP, PDF 등)을 자동으로 다운로드하여 텍스트로 파싱합니다.
3. **stage 3 (판단)**: 파싱된 본문을 활용하여 8대 영역 분석 프롬프트를 자동으로 조립합니다.
4. **LLM 연동 및 카드 생성 (Human-in-the-loop)**:
   - 모달 내부의 **① 분석 프롬프트** 영역에서 `📋 프롬프트 복사`를 클릭합니다.
   - 선호하는 LLM(예: Claude 등)에 붙여넣고 질문을 전송합니다.
   - LLM이 생성한 마크다운 형식의 분석카드 전체를 복사하여 **② 붙여넣기창**에 붙여넣은 뒤 `카드 저장·렌더`를 클릭합니다.
   - 생성된 카드는 `analysis/<공고번호>_분석카드.md`에 저장되고 화면에 즉시 표시됩니다.

### Step 4: 착수 → 제안서 생성 (2026-07-21 R8 개정)
* ⚠️ **대시보드에는 더 이상 "제안서 생성" 클릭 버튼이 없습니다**(옛 `/api/render`·`/api/storyline-prompt`는 W31에서 제거·Reuse 격리됨). 클릭형 실행 트리거는 혼란을 줄 수 있어 정리 대상이 되었습니다.
* **정본 착수 통로**: 공고 카드의 **공고 고유번호 복사 버튼**을 눌러 번호를 복사 → 이 채팅창(Claude 세션)에 붙여넣습니다. 그 붙여넣기 자체가 착수 판단(A1)의 실행 신호입니다.
* 그 뒤 세션(또는 사람)이 CLI **동사 3개**로 진행합니다:
  ```powershell
  python proposal_system/scripts/proposal_pipeline.py start --bid <공고번호> --mode direct   # 기본. 외부 미노출이 꼭 필요하면 --mode secure
  python proposal_system/scripts/proposal_pipeline.py go --run <run>                         # 다음 체크포인트까지 자동 진행(멱등)
  python proposal_system/scripts/proposal_pipeline.py ship --run <run> --pptx                # 승인 + PPTX
  ```
* 진행 중 위치·다음 할 일이 궁금하면 문서를 뒤지지 말고 `status --run <run>`을 실행하세요 — 코드가 답합니다. 상세 공정 = `MANUAL.md`.

---

## 4. 무인 정기 공고수집 (`collect_job.py`)
직접 대시보드에서 검색하지 않아도, 백그라운드에서 주기적으로 나라장터 입찰공고를 자동 수집하고 다이제스트를 갱신할 수 있는 무인 스케줄러가 포함되어 있습니다.

* **실행 명령어**:
  ```powershell
  py -3 scheduler\collect_job.py
  ```
* **동작 원리**: `config.json`에 정의된 `collect_keywords` 목록(기본 `["AI"]`)을 순회하며 `collector.py`를 실행하고, 신규 공고를 `bids.db`에 적재한 뒤 `digest/` 폴더 내에 마크다운 다이제스트를 자동 갱신합니다.
* **Windows 작업 스케줄러 등록**: 주기적인 백그라운드 구동 방법은 `scheduler/README.md` 가이드라인의 *Windows Task Scheduler setup* 섹션을 참고하세요.

---

## 5. 회귀 테스트 실행
시스템이 올바르게 통합 및 구동되고 있는지 검증하는 회귀(스모크) 테스트입니다. (네트워크 의존성 없음)

```powershell
python dashboard/test_smoke.py
```
* 핵심 기능 회귀를 전건 확인합니다(상태머신·브리핑·평가·이미지 렌더 라우트 테스트 포함. 정확한 수는 실행 출력이 정본 — 2026-07-21 현재 265종).
* ⚠️ 스모크 통과 ≠ 대시보드 UI 경로 검증. 스모크는 네트워크 없이 모듈 단위를 돌린다. 실제 관문 흐름은 브라우저로 정주행해야 확인된다(2026-07-08 S1 정주행이 관문4 프롬프트 조회 버그를 그렇게 발견).
